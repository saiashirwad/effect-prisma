// This file was generated by effect-prisma, do not edit manually.

/* eslint-disable @typescript-eslint/no-explicit-any */
import type { Prisma, PrismaClient } from "@prisma/client";
import { Effect, Option } from "effect";

import {
  PrismaNotFoundError,
  PrismaCreateError,
  PrismaUpdateError,
  PrismaDeleteError,
  PrismaQueryError,
  parsePrismaErrorKind,
} from "effect-prisma/runtime";

type Exec = <A, Err extends Error>(
  f: (db: PrismaClient) => Promise<A>,
  errCallback: (cause: unknown) => Err,
) => Effect.Effect<A, Err>;

export interface ProjectRepository {
  findFirst: <T extends Prisma.ProjectFindFirstArgs, E = PrismaNotFoundError>(
    args?: T,
    mapError?: (e: PrismaNotFoundError) => E,
  ) => Effect.Effect<Prisma.ProjectGetPayload<T>, E>;

  findUnique: <T extends Prisma.ProjectFindUniqueArgs, E = PrismaNotFoundError>(
    args: T,
    mapError?: (e: PrismaNotFoundError) => E,
  ) => Effect.Effect<Prisma.ProjectGetPayload<T>, E>;

  findMany: <T extends Prisma.ProjectFindManyArgs, E = PrismaQueryError>(
    args?: T,
    mapError?: (e: PrismaQueryError) => E,
  ) => Effect.Effect<Prisma.ProjectGetPayload<T>[], E>;

  create: <T extends Prisma.ProjectCreateArgs, E = PrismaCreateError>(
    args: T,
    mapError?: (e: PrismaCreateError) => E,
  ) => Effect.Effect<Prisma.ProjectGetPayload<T>, E>;

  createMany: <T extends Prisma.ProjectCreateManyArgs, E = PrismaCreateError>(
    args: T,
    mapError?: (e: PrismaCreateError) => E,
  ) => Effect.Effect<Prisma.BatchPayload, E>;

  createManyAndReturn: <T extends Prisma.ProjectCreateManyAndReturnArgs, E = PrismaCreateError>(
    args: T,
    mapError?: (e: PrismaCreateError) => E,
  ) => Effect.Effect<Prisma.ProjectGetPayload<T>[], E>;

  update: <T extends Prisma.ProjectUpdateArgs, E = PrismaUpdateError>(
    args: T,
    mapError?: (e: PrismaUpdateError) => E,
  ) => Effect.Effect<Prisma.ProjectGetPayload<T>, E>;

  updateMany: <T extends Prisma.ProjectUpdateManyArgs, E = PrismaUpdateError>(
    args: T,
    mapError?: (e: PrismaUpdateError) => E,
  ) => Effect.Effect<Prisma.BatchPayload, E>;

  upsert: <T extends Prisma.ProjectUpsertArgs, E = PrismaCreateError>(
    args: T,
    mapError?: (e: PrismaCreateError) => E,
  ) => Effect.Effect<Prisma.ProjectGetPayload<T>, E>;

  delete: <T extends Prisma.ProjectDeleteArgs, E = PrismaDeleteError>(
    args: T,
    mapError?: (e: PrismaDeleteError) => E,
  ) => Effect.Effect<Prisma.ProjectGetPayload<T>, E>;

  deleteMany: <T extends Prisma.ProjectDeleteManyArgs, E = PrismaDeleteError>(
    args?: T,
    mapError?: (e: PrismaDeleteError) => E,
  ) => Effect.Effect<Prisma.BatchPayload, E>;

  count: <T extends Prisma.ProjectCountArgs, E = PrismaQueryError>(
    args?: T,
    mapError?: (e: PrismaQueryError) => E,
  ) => Effect.Effect<number, E>;
}

export function createProjectRepository(exec: Exec): ProjectRepository {
  return {
    findFirst: (args, mapError) => {
      const effect = exec(
        db => db.project.findFirstOrThrow(args ?? ({} as any)),
        cause => new PrismaNotFoundError({ kind: parsePrismaErrorKind(cause), cause, table: "Project", where: Option.fromNullable(args?.where) }),
      ) as any;
      return mapError ? Effect.mapError(effect, mapError) : effect;
    },

    findUnique: (args, mapError) => {
      const effect = exec(
        db => db.project.findUniqueOrThrow(args),
        cause => new PrismaNotFoundError({ kind: parsePrismaErrorKind(cause), cause, table: "Project", where: Option.some(args.where) }),
      ) as any;
      return mapError ? Effect.mapError(effect, mapError) : effect;
    },

    findMany: (args, mapError) => {
      const effect = exec(
        db => db.project.findMany(args ?? ({} as any)),
        cause => new PrismaQueryError({ kind: parsePrismaErrorKind(cause), cause, table: "Project", where: Option.fromNullable(args?.where) }),
      ) as any;
      return mapError ? Effect.mapError(effect, mapError) : effect;
    },

    create: (args, mapError) => {
      const effect = exec(
        db => db.project.create(args),
        cause => new PrismaCreateError({ kind: parsePrismaErrorKind(cause), cause, table: "Project", data: args.data as any }),
      ) as any;
      return mapError ? Effect.mapError(effect, mapError) : effect;
    },

    createMany: (args, mapError) => {
      const effect = exec(
        db => db.project.createMany(args),
        cause => new PrismaCreateError({ kind: parsePrismaErrorKind(cause), cause, table: "Project", data: args.data as any }),
      ) as any;
      return mapError ? Effect.mapError(effect, mapError) : effect;
    },

    createManyAndReturn: (args, mapError) => {
      const effect = exec(
        db => db.project.createManyAndReturn(args),
        cause => new PrismaCreateError({ kind: parsePrismaErrorKind(cause), cause, table: "Project", data: args.data as any }),
      ) as any;
      return mapError ? Effect.mapError(effect, mapError) : effect;
    },

    update: (args, mapError) => {
      const effect = exec(
        db => db.project.update(args),
        cause => new PrismaUpdateError({ kind: parsePrismaErrorKind(cause), cause, table: "Project", where: args.where as any, data: args.data as any }),
      ) as any;
      return mapError ? Effect.mapError(effect, mapError) : effect;
    },

    updateMany: (args, mapError) => {
      const effect = exec(
        db => db.project.updateMany(args),
        cause => new PrismaUpdateError({ kind: parsePrismaErrorKind(cause), cause, table: "Project", where: args.where as any, data: args.data as any }),
      ) as any;
      return mapError ? Effect.mapError(effect, mapError) : effect;
    },

    upsert: (args, mapError) => {
      const effect = exec(
        db => db.project.upsert(args),
        cause => new PrismaCreateError({ kind: parsePrismaErrorKind(cause), cause, table: "Project", data: args.create as any }),
      ) as any;
      return mapError ? Effect.mapError(effect, mapError) : effect;
    },

    delete: (args, mapError) => {
      const effect = exec(
        db => db.project.delete(args),
        cause => new PrismaDeleteError({ kind: parsePrismaErrorKind(cause), cause, table: "Project", where: Option.some(args.where) }),
      ) as any;
      return mapError ? Effect.mapError(effect, mapError) : effect;
    },

    deleteMany: (args, mapError) => {
      const effect = exec(
        db => db.project.deleteMany(args ?? ({} as any)),
        cause => new PrismaDeleteError({ kind: parsePrismaErrorKind(cause), cause, table: "Project", where: Option.fromNullable(args?.where) }),
      ) as any;
      return mapError ? Effect.mapError(effect, mapError) : effect;
    },

    count: (args, mapError) => {
      const effect = exec(
        db => db.project.count(args ?? ({} as any)),
        cause => new PrismaQueryError({ kind: parsePrismaErrorKind(cause), cause, table: "Project", where: Option.fromNullable(args?.where) }),
      ) as any;
      return mapError ? Effect.mapError(effect, mapError) : effect;
    },
  };
}
